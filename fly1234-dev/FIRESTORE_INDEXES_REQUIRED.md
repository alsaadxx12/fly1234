# Firestore Indexes Required

لتفعيل فلترة التذاكر حسب الموظف، يجب إنشاء الفهارس التالية في Firebase Console:

## 📌 Index 1: tickets - createdBy + createdAt

**Collection:** `tickets`

**Fields:**
1. `createdBy` (Ascending)
2. `createdAt` (Descending)

**Query Scope:** Collection

---

## 🔧 كيفية إنشاء الفهرس:

### الطريقة الأولى: عبر Firebase Console

1. اذهب إلى Firebase Console: https://console.firebase.google.com
2. اختر المشروع الخاص بك
3. اذهب إلى **Firestore Database**
4. اختر تبويب **Indexes**
5. اضغط على **Create Index**
6. املأ البيانات التالية:
   - Collection ID: `tickets`
   - Field 1: `createdBy` - Ascending
   - Field 2: `createdAt` - Descending
   - Query scope: Collection
7. اضغط على **Create**

---

### الطريقة الثانية: تلقائياً عند التشغيل

عند تشغيل التطبيق لأول مرة بعد التحديث، إذا ظهر خطأ في Console مثل:

```
The query requires an index. You can create it here: https://console.firebase.google.com/...
```

اضغط على الرابط الموجود في الخطأ، وسيتم توجيهك مباشرة لإنشاء الفهرس.

---

## ✅ التأكد من إنشاء الفهرس:

1. اذهب إلى **Firestore Database** > **Indexes**
2. تأكد من وجود فهرس `tickets` مع الحقول `createdBy` و `createdAt`
3. تأكد من أن حالة الفهرس **Enabled** (وليس Building)

---

## 📊 ماذا يفعل هذا التحديث؟

بعد هذا التحديث:

### 📋 صفحة التذاكر (Tickets):
✅ **كل موظف يرى فقط التذاكر التي أدخلها**
✅ **الموظف علي لا يرى تذاكر الموظف حسين**
✅ **الموظف حسين لا يرى تذاكر الموظف علي**
✅ **إذا لم يكن للموظف إدخالات، يظهر الجدول فارغ**

### 🔍 صفحة التدقيق (Audit):
✅ **تعرض جميع التذاكر من كل الموظفين (بدون فلتر)**
✅ **المدقق يرى تذاكر علي + حسين + جميع الموظفين**

---

## 🔍 الملفات المحدثة:

1. `src/pages/Tickets/hooks/useTickets.ts` - إضافة فلتر `where('createdBy', '==', user.uid)` ✅
2. `src/pages/Audit/useAuditTickets.ts` - **بدون فلتر** (تعرض جميع التذاكر) ✅

---

## ⚠️ ملاحظات مهمة:

- الفهرس يستغرق بضع دقائق للإنشاء (حسب حجم البيانات)
- بعد إنشاء الفهرس، انتظر حتى تصبح حالته **Enabled**
- إذا كانت البيانات الموجودة كبيرة، قد يستغرق الفهرس وقتاً أطول
- لا تحتاج لإعادة رفع التطبيق بعد إنشاء الفهرس

---

## 🎯 النتيجة النهائية:

### 📋 صفحة التذاكر (Tickets):
```
┌─────────────────────────────────────────┐
│ الموظف: علي                            │
├─────────────────────────────────────────┤
│ التذاكر الخاصة به فقط:                │
│ - تذكرة 1 (أدخلها علي)                │
│ - تذكرة 2 (أدخلها علي)                │
│ - تذكرة 3 (أدخلها علي)                │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ الموظف: حسين                           │
├─────────────────────────────────────────┤
│ التذاكر الخاصة به فقط:                │
│ - تذكرة 4 (أدخلها حسين)               │
│ - تذكرة 5 (أدخلها حسين)               │
└─────────────────────────────────────────┘
```

### 🔍 صفحة التدقيق (Audit):
```
┌─────────────────────────────────────────┐
│ المدقق: أحمد                           │
├─────────────────────────────────────────┤
│ يرى جميع التذاكر:                      │
│ - تذكرة 1 (أدخلها علي)                │
│ - تذكرة 2 (أدخلها علي)                │
│ - تذكرة 3 (أدخلها علي)                │
│ - تذكرة 4 (أدخلها حسين)               │
│ - تذكرة 5 (أدخلها حسين)               │
└─────────────────────────────────────────┘
```

**في صفحة التذاكر: كل موظف لديه بياناته الخاصة ومعزولة تماماً!** ✅
**في صفحة التدقيق: المدقق يرى جميع التذاكر من كل الموظفين!** ✅
