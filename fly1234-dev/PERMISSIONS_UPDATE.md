# تحديث نظام الصلاحيات - إضافة الأرصدة و API

## التحديثات المضافة

تم إضافة صفحتين جديدتين إلى نظام الصلاحيات:

### 1. صفحة الأرصدة (Balances)
- **الاسم العربي**: الأرصدة
- **الاسم الإنجليزي**: balances
- **الأيقونة**: DollarSign 💰
- **الصلاحيات المتاحة**:
  - عرض (view)
  - إضافة (add)
  - تعديل (edit)
  - حذف (delete)

### 2. صفحة ربط API
- **الاسم**: API
- **الاسم الإنجليزي**: api
- **الأيقونة**: Link 🔗
- **الصلاحيات المتاحة**:
  - عرض (view)
  - إضافة (add)
  - تعديل (edit)
  - حذف (delete)

## الملفات المحدثة

### 1. `/src/pages/Employees.tsx`
تم إضافة الصفحتين الجديدتين إلى قائمة `webWindows`:

```typescript
const webWindows = [
  // ... الصفحات السابقة
  { id: 7, name: 'الأرصدة', permissions: ['view', 'add', 'edit', 'delete'] },
  { id: 8, name: 'API', permissions: ['view', 'add', 'edit', 'delete'] },
  // ...
];
```

### 2. `/src/pages/Employees/components/PermissionEditor.tsx`
تم إضافة الأيقونات المناسبة:

```typescript
import { DollarSign, Link } from 'lucide-react';

const getWindowIcon = (windowName: string) => {
  // ...
  if (windowName === 'الأرصدة') return <DollarSign className="w-4 h-4 text-indigo-600" />;
  if (windowName === 'API') return <Link className="w-4 h-4 text-indigo-600" />;
  // ...
};
```

### 3. `/src/components/PermissionGuard.tsx`
تم إضافة التطابق بين الأسماء العربية والإنجليزية:

```typescript
const pageNameMap: Record<string, string> = {
  // ... الصفحات السابقة
  'balances': 'الأرصدة',
  'الأرصدة': 'balances',
  'api': 'API',
  'API': 'api',
  // ...
};
```

### 4. `/src/App.tsx`
تم تحديث حماية الصلاحيات للصفحات:

```typescript
<Route path="/balances" element={
  <PermissionGuard requiredPermissions={{ page: 'الأرصدة', actions: ['view'] }}>
    <Balances />
  </PermissionGuard>
} />

<Route path="/api-integrations" element={
  <PermissionGuard requiredPermissions={{ page: 'API', actions: ['view'] }}>
    <ApiIntegrations />
  </PermissionGuard>
} />
```

## كيفية الاستخدام

### 1. إنشاء مجموعة صلاحيات جديدة
1. افتح صفحة **الموظفين**
2. انتقل إلى تبويب **الصلاحيات**
3. اضغط على **إضافة مجموعة جديدة**
4. ستجد الآن صفحتين جديدتين:
   - **الأرصدة** (مع أيقونة الدولار 💰)
   - **API** (مع أيقونة الربط 🔗)

### 2. تعديل صلاحيات مجموعة موجودة
1. اضغط على **تعديل** لأي مجموعة صلاحيات
2. قم بتفعيل أو تعطيل الصلاحيات المطلوبة:
   - **الأرصدة**: للتحكم في صفحة إدارة الأرصدة
   - **API**: للتحكم في صفحة ربط API

### 3. تطبيق الصلاحيات على الموظفين
1. عند إضافة أو تعديل موظف
2. اختر مجموعة الصلاحيات التي تحتوي على الصلاحيات المناسبة
3. سيتمكن الموظف من الوصول فقط للصفحات المسموح بها

## الصلاحيات المتاحة

### لصفحة الأرصدة:
- ✅ **عرض**: مشاهدة قائمة الأرصدة
- ➕ **إضافة**: إضافة رصيد جديد
- ✏️ **تعديل**: تعديل الأرصدة الموجودة
- ❌ **حذف**: حذف الأرصدة

### لصفحة API:
- ✅ **عرض**: مشاهدة اتصالات API
- ➕ **إضافة**: إضافة اتصال API جديد
- ✏️ **تعديل**: تعديل اتصالات API
- ❌ **حذف**: حذف اتصالات API

## مثال على الاستخدام

### سيناريو 1: مدير الحسابات
```
الصلاحيات المطلوبة:
- الأرصدة: عرض، إضافة، تعديل ✅
- API: عرض فقط ✅
```

### سيناريو 2: مدير النظام
```
الصلاحيات المطلوبة:
- الأرصدة: جميع الصلاحيات ✅
- API: جميع الصلاحيات ✅
```

### سيناريو 3: موظف عرض فقط
```
الصلاحيات المطلوبة:
- الأرصدة: عرض فقط ✅
- API: بدون صلاحيات ❌
```

## الحماية الأمنية

- ✓ جميع الصفحات محمية بـ `PermissionGuard`
- ✓ لا يمكن الوصول للصفحة بدون الصلاحية المناسبة
- ✓ يتم إعادة التوجيه للوحة التحكم في حالة عدم وجود صلاحية
- ✓ رسالة واضحة للمستخدم عند محاولة الوصول لصفحة غير مصرح بها

## الترتيب في القائمة

الصفحات مرتبة كالتالي:
1. الشركات
2. الموظفين
3. الحسابات
4. الصناديق
5. لوحة التحكم
6. الإعلانات
7. **الأرصدة** ← جديد
8. **API** ← جديد
9. الإعدادات

## ملاحظات مهمة

1. **التوافق مع الصلاحيات القديمة**: جميع الصلاحيات السابقة تعمل بشكل طبيعي
2. **التحديثات التلقائية**: عند إضافة مجموعة صلاحيات جديدة، ستظهر الصفحات الجديدة تلقائياً
3. **مجموعات Admin**: المدراء لديهم صلاحيات كاملة على جميع الصفحات
4. **الأيقونات**: تم اختيار أيقونات واضحة ومعبرة لسهولة التمييز

---

**تم الاختبار**: جميع التحديثات تعمل بشكل صحيح ✅
**تاريخ التحديث**: 2025-10-12
